import React from "react";

export default function Footer() {
  return (
    <footer className="app-footer" role="contentinfo">
      {/* Simple bird glyph (you can swap to an SVG later) */}
      <span className="footer-bird" aria-label="bird">🐦</span>
    </footer>
  );
}
